---

---

<section class="offer" id="offer">
  <div class="offer__container">
    <div class="offer__card animate-on-scroll">
      <span class="offer__label">Exkluzivní nabídka</span>
      <h2 class="offer__heading">Máme pro vás něco speciálního</h2>
      <p class="offer__subtext">
        Připravili jsme nabídku, kterou jinde nenajdete. Zapište se na&nbsp;čekací listinu a&nbsp;odhalíte ji.
      </p>
      <a href="#waitlist" class="btn-gold offer__cta">Zapsat se a&nbsp;odhalit nabídku</a>
      <div class="offer__urgency">
        <svg width="18" height="18" viewBox="0 0 18 18" fill="none" stroke="var(--color-gold)" stroke-width="1.5" aria-hidden="true">
          <circle cx="9" cy="9" r="7.5"/>
          <path d="M9 5v4l2.5 1.5"/>
        </svg>
        <span>Platí pouze po&nbsp;omezenou dobu. Počet kusů je limitovaný.</span>
      </div>
    </div>
  </div>
</section>

<style>
  .offer {
    padding: var(--space-2xl) var(--space-lg);
    background-color: var(--color-bg-secondary);
  }

  .offer__container {
    max-width: 600px;
    margin: 0 auto;
  }

  .offer__card {
    text-align: center;
    padding: var(--space-xl);
    border: 2px solid rgba(212, 168, 67, 0.3);
    border-radius: 16px;
    background: linear-gradient(135deg, rgba(212, 168, 67, 0.06) 0%, rgba(212, 168, 67, 0.02) 100%);
  }

  .offer__label {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.4em;
    text-transform: uppercase;
    color: var(--color-gold);
    margin-bottom: var(--space-lg);
    border: 1px solid rgba(212, 168, 67, 0.4);
    padding: 0.4rem 1.2rem;
    border-radius: 50px;
  }

  .offer__heading {
    font-size: clamp(1.75rem, 5vw, 2.8rem);
    color: var(--color-text-primary);
    margin-bottom: var(--space-lg);
  }

  .offer__subtext {
    color: var(--color-text-secondary);
    font-size: 1.1rem;
    line-height: 1.7;
    margin-bottom: var(--space-xl);
    max-width: 450px;
    margin-left: auto;
    margin-right: auto;
  }

  .offer__cta {
    display: inline-block;
    padding: 1.2rem 3rem;
    font-size: 1.2rem;
    margin-bottom: var(--space-lg);
    animation: pulse-glow 3s ease-in-out infinite;
  }

  .offer__urgency {
    display: flex;
    align-items: flex-start;
    justify-content: center;
    gap: 0.5rem;
    font-size: 0.85rem;
    color: var(--color-gold);
    font-weight: 500;
    padding-top: var(--space-sm);
  }

  @media (max-width: 640px) {
    .offer {
      padding: 2.5rem 1rem;
    }

    .offer__card {
      padding: 1.5rem 1.25rem;
    }

    .offer__label {
      font-size: 0.65rem;
      letter-spacing: 0.25em;
      padding: 0.3rem 1rem;
      margin-bottom: 1rem;
    }

    .offer__heading {
      font-size: 1.5rem;
      margin-bottom: 0.75rem;
    }

    .offer__subtext {
      font-size: 0.95rem;
      line-height: 1.6;
      margin-bottom: 1.5rem;
    }

    .offer__cta {
      display: block;
      padding: 1rem 1.5rem;
      font-size: 1.05rem;
      margin-bottom: 1rem;
    }

    .offer__urgency {
      font-size: 0.8rem;
    }
  }
</style>
